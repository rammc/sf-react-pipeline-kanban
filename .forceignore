# List files or directories below to ignore them when running force:source:push, force:source:pull, and force:source:status
# More information: https://developer.salesforce.com/docs/atlas.en-us.sfdx_dev.meta/sfdx_dev/sfdx_dev_exclude_source.htm
#

package.xml

# LWC configuration files
**/jsconfig.json
**/.eslintrc.json

# LWC Jest
**/__tests__/**

# Multi-Framework UIBundle source — the .uibundle-meta.xml metadata type
# is not yet in the source-deploy-retrieve registry as of CLI v2.118 +
# plugin-ui-bundle-dev v1.2.2 (Multi-Framework Beta). Bundles are run
# locally via `sf ui-bundle dev` for now; remove this ignore once the
# beta exits and the registry catches up.
**/uiBundles/**
**/node_modules/**